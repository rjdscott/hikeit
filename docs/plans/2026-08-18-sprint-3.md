# Sprint 3 — timing, presenting, hardening

**Status:** In progress (2026-08-18).

## Scope (chosen from the "best in market" list; Rob said "continue")
1. **Puff with crew reaction** — overlay two trajectories: crew already hiked vs crew who go from sitting → full hike N seconds after the puff hits (reaction slider 0–4 s). Shows *why hike before it hits*: peak heel difference, time spent above 30°.
2. **Presenter mode** — toggle: larger type, hides equations/advanced/crew list, keeps deck + stern + stats + charts; keyboard ←/→ drive the lesson; `#present=1` in URL.
3. **e2e in CI** — Playwright chromium install step in deploy.yml; e2e runs against `vite preview` before build artefact upload.
4. Small: screenshot script uses `load` (SW keeps network busy); Advanced panel exposes roll inertia/damping (plan item from sprint 2).

## Out of scope (next): ORC certificate importer, cross-boat comparison, gust timeline scrubber.
