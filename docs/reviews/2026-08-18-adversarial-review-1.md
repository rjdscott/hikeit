# Adversarial review #1 — 2026-08-18

Workflow: 4 reviewer lenses (physics, sailor realism, code/React/mobile, UX/design) → each finding challenged by 2 independent refuters (confirmed only if both fail to refute). 28 findings, 23 confirmed, 5 refuted. All confirmed items were fixed in the follow-up commit unless noted.

## Confirmed
- **[high] (physics) Crew height penalty double-counts the ORC default crew already inside the certificate RM₁ / Δ_sailing** — `src/physics/stability.ts`
  - Claim: ORC VPP §4.2.2 (verified in the 2023 PDF): sailing-trim displacement and VCG — the condition in which the certificate GZ curve, RM at 1° and LPS are computed — already include the DEFAULT CREW WEIGHT (CW = 25.8·LSM0^1.4262 ≈ 935 kg for the Xp 44) on the centreline at crewv = 0.05·LSM0 + 0.36 ≈ 0.98 m above the waterplane. The app's Δ = 9700 kg is that number (light 8650 + crew 935 + gear ~150), yet xp44.json/Equations label it 'sailing displacement without crew', zG = −0.1 m is a bare-boat VCG, and crewMoment() charges every crew member −m g (z − zG) sinφ with z − zG ≈ 1.5 m. Physically the ce…
  - Fix applied: Add stability.crewRefZ (= 0.05·LSM0 + 0.36 ≈ 0.98 m, or expose in json) and compute the height term as (p.z − crewRefZ)·sinφ in crewMoment (keep the zPenalty toggle); relabel Δ as 'ORC sailing displacement (incl. default crew)'; in combinedCg use total = b.disp (crew already inside, or disp − 935 + Σm) and shift zG from crewRefZ rather than b.zG; update lesson step 3 (break-even ≈ 78°, penalty ~8%…
- **[low] (physics) Effective rig height omits ORC kheff (and KPP≈0.014, not 0.005): induced drag ~1.7× too high upwind, drive force ~10% low** — `src/physics/boat.ts`
  - Claim: resolveBoat sets hEff = 1.1·(P+BAS+freeboard) = 22.4 m (AR 4.5). ORC eq 5.42–5.43: heff = geometric span × eff_span_corr(≈1.1) × kheff(AWA), with kheff = 1.45 at 20° falling to 0.80 at 80°; at the upwind β≈25° kheff ≈1.3 → heff ≈ 29–30 m, which is the value the project's own research doc (§4.2, h_eff ≈ 30 m, C_D ≈ 0.11, C_R ≈ 0.56) assumes. Also ORC's quadratic parasite term KPP is ~0.014 (kpmm 0.01379) not 0.005. With the code's numbers at β=27°: C_Di = 0.155, C_D = 0.195, C_H = 1.36, C_R = 0.474; with heff 30 m / KPP 0.014: C_D ≈ 0.15, C_H ≈ 1.34, C_R ≈ 0.52 → the 'Drive force' payoff readou…
  - Fix applied: Multiply hEff by a kheff(awa) lookup in sailCoeffs (linear 1.45@20° → 0.80@80°, clamped) or, minimally, use hEff ≈ 1.1·1.3·(P+BAS+freeboard) for the upwind mode; set the quadratic parasite constant to ~0.014 (main 0.0138, jib ~0.016 area-weighted); re-baseline the C_H test band.
- **[low] (physics) Equations panel C_H identity is false when the Advanced 'heeling coefficient scale' ≠ 1** — `src/ui/Equations.tsx`
  - Claim: sailCoeffs returns ch = chScale·(C_L cosβ + C_D sinβ) but the KaTeX line prints 'C_H = C_L cosβ + C_D sinβ = cl·cosβ + cd·sinβ = ch' using d.coeffs.ch. With chScale = 1.3 the rendered arithmetic reads e.g. 1.43·0.90 + 0.20·0.43 = 1.77 (true sum 1.37), and the HM row silently uses the scaled value.
  - Fix applied: Render the unscaled sum, then append '× k_H = ch' when boat.chScale ≠ 1 (or show C_H = k_H·(…) with k_H substituted).
- **[low] (physics) Research doc apparent-wind formula has the wrong sign (code and Equations panel are correct)** — `docs/research/02-physics-model.md`
  - Claim: §4.5 states V_aw² = V_tw² + V_b² − 2 V_tw V_b cos(TWA). With TWA measured from the bow the crosswind/headwind decomposition gives V_aw² = (V_tw cosTWA + V_b)² + (V_tw sinTWA)² = V_tw² + V_b² + 2 V_tw V_b cosTWA (ORC eq 7.3), which is what aero.ts apparentWind and Equations.tsx implement (TWS 12/TWA 40/BSP 7 → 17.9 kn, not 8.6 kn).
  - Fix applied: Change the sign in §4.5 to '+ 2 V_tw V_b cos(TWA)' so the doc matches code and ORC eq 7.3.
- **[high] (realism) Crew height penalty referenced to hull VCG while Δs=9700 kg already contains the ORC default crew — double-counts crew mass and overstates the z-term ~4×** — `src/physics/stability.ts (crewMoment / combinedCg), src/data/xp44.json (stability.zG, dispSailing), src/ui/Equations.tsx caption, src/data/lessons.ts step 5`
  - Claim: ORC 'sailing trim' displacement and the certificate RM at 1° INCLUDE default crew weight (VPP doc §4.2.2: crew + gear + sails added, crew VCG crewv = 0.05·LSM0+0.36 ≈ 0.9 m above WL; §4.4.1 GZ curve is 'for the boat in sailing displacement'; research 01 confirms 8650 light → 9654–10382 sailing). The app uses Δs=9700/RM1=260 for RM_hull, then adds 850 kg again as point masses with penalty −(z − z_G)·sinφ, z_G=−0.1. Consequences (numbers from running the model): hiked crew at 20°: gross 14.66 kN·m, model net 10.66 (penalty 4.0); correct increment relative to the certificate basis is −m·g·(z − 0.…
  - Fix applied: Treat 9700 kg as ORC sailing trim (includes ~930 kg crew at z_crew0 ≈ 0.05·LSM0+0.36 ≈ 0.9 m, centreline). Add stability.zCrew0 (0.9) to xp44.json and change crewMoment to m·g·(y·cosφ − (z − zCrew0)·sinφ); combinedCg total = disp (not disp+Σm), yG = Σm·y/disp; fix the Equations caption ('Δ = 9700 kg ORC sailing trim incl. default crew') and stern-view Δg; rewrite lesson step 5 (penalty small until…
- **[medium] (realism) Reaching lesson prose contradicts the model: hiking matters MORE (in degrees) at 75–90° TWA, and the heeling arm does not change with TWA** — `src/data/lessons.ts step 8 ('Reaching, and the fine print')`
  - Claim: Prose: 'C_H drops and the heeling arm changes, so hiking matters less than upwind'. Model at 14 kn: TWA 40 hike 31.3° vs below 34.5° (Δ3.2°); TWA 75 21.8 vs 26.1 (Δ4.3°); TWA 90 14.0 vs 19.2 (Δ5.2°). Δheel per crew grows reaching because the boat sits on the stiff low-heel part of the GZ curve. heelingArm() depends only on flat, so 'arm changes' is false in-app (only Code 0 mode changes zce). Also the step lands at TWA 75 flat 1 with 21.8° — a coach reading the numbers will see hiking bought MORE degrees than upwind.
  - Fix applied: Rewrite: 'Reaching, C_H falls (1.36 → 1.13 at 75°) so absolute heel drops; each rail body still buys 0.4–0.5° — you are lower on the GZ curve where degrees are cheap. What changes is the payoff: reaching, drive comes from C_R, not from holding heel.' Drop 'heeling arm changes' or make it about the Code 0 CE.
- **[medium] (realism) Rail seat arms (1.63–1.68 m) are ~0.2 m short of both the research table and ORC's default rail arm; full hike barely reaches ORC's baseline** — `src/data/xp44.json deck.rail.inset (0.35), src/physics/types.ts POSTURE_OFFSET, physics.test.ts arm bound`
  - Claim: buildSlots gives y = halfbeam − 0.35 = 1.63–1.68 m sitting, 1.83–1.88 legs, 2.03–2.08 hike; mass-averaged 10-crew arm 1.55 (sit) / 1.85 (hike). Research 02 §2.2 specifies 1.85 / 2.05 / 2.25 and ORC eq. 4.30 CARM = average half-beam over occupied side deck ≈ 2.0 m (deck edge) with +0.5 m for sportboat hiking straps. A racer sitting on the toerail with legs under the lower lifeline has CG ~0.1–0.15 m inboard of the deck edge, i.e. ~1.9 m at max beam. Net: crew RM understated ~10–15% (on top of finding #1), which is why the app shows 850 kg on the rail as only 6–9% of hull RM at typical heel; and…
  - Fix applied: Set rail.inset ≈ 0.15 (deck edge minus half a body), keep +0.2/+0.4 postures (→ ~1.9/2.1/2.3 m, matching research and RRS 49.2 max), and make the racing preset default to 'legs' (legs over, torso inside) with 'hike' as the RRS-limited max. Update the 1.6–2.1 test bound and lesson step 3's '~1.8 m'.
- **[low] (realism) Lesson prose numbers don't match what the model displays** — `src/data/lessons.ts steps 3, 4, 6`
  - Claim: Step 3: 'rail ~1.8 m off centreline' — table shows +1.63…+1.68 (helm 1.27, trimmer 0.96; mass-avg 1.55). Step 4: 'legs over … another ~0.2 m; a full legal hike … another ~0.4 m' reads as +0.6 total, code is +0.4 total (+0.2 over legs). Step 6: 'roughly 2–3 kn more breeze' — model at step-6 state (16 kn, flat 1, hiked) shows free wind +1.8 kn (test only asserts >1); after fixing #1 it rises to ~2.2. Step 3 'equilibrium point drop several degrees' = 2.8° at 12 kn (below→sit).
  - Fix applied: Read numbers from the model in the prose or soften: '~1.7 m (deck edge less body width)', 'full hike is +0.4 m over sitting (+0.2 over legs-over)', 'about 2 kn (more once the trimmers depower)', 'about 3°'. Ideally template the values from `derive()` so prose can't drift.
- **[high] (code) Out-of-range or non-integer #step= in URL crashes the whole app** — `src/ui/Lesson.tsx`
  - Claim: decodeHash (state.ts:110) clamps `step` to 0..20 but LESSONS has 8 entries and the value is not integer-checked. App.init only applies the lesson patch when LESSONS[step] exists but leaves lessonStep as-is, so App renders <Lesson>, which does `const step = LESSONS[i]; step.title` (Lesson.tsx:7,17) on undefined. Verified: loading http://localhost:5179/hikeit/#step=12 → `TypeError: Cannot read properties of undefined (reading 'title')`, React unmounts the tree (no error boundary) → blank page. `#step=2.5` hits the same path.
  - Fix applied: In App.init: `if (s.lessonStep !== null && !LESSONS[s.lessonStep]) s = { ...s, lessonStep: null }` (or clamp in decodeHash with `Math.floor` and `LESSONS.length-1` — but state.ts→lessons.ts would be a circular import, so App.init is the lazy spot). Also guard Lesson: `if (i === null || !LESSONS[i]) return null`. Add a state.test case for `#step=99` / `#step=2.5`.
- **[high] (code) Crew weight input clamps on every keystroke, so weights cannot be typed** — `src/ui/Readouts.tsx`
  - Claim: Readouts.tsx:38 dispatches `Math.min(150, Math.max(40, Number(value) || 0))` on each onChange of a controlled input. Typing '9' (after select-all) yields 40, the next digit appends → '402' → 150. Verified with Playwright: select-all + type '92' leaves the field at 150. Only the spinner arrows / paste work; on mobile keyboards it is effectively unusable.
  - Fix applied: Dispatch the raw `Number(e.target.value)` on change (ignore NaN), and clamp on blur (`onBlur={() => dispatch({type:'setCrew', id, patch:{kg: clamp(c.kg)}})}`), or clamp inside the reducer only when the value is outside 40..150 AND the field is not mid-edit. Alternatively keep a local string state and commit on blur/Enter.
- **[medium] (code) history.replaceState on every state change: Safari/iOS throws SecurityError after 100 calls, effect throw unmounts the app** — `src/App.tsx`
  - Claim: App.tsx:31-34 calls history.replaceState synchronously in a useEffect on every `s` change. Range sliders (`onChange` = native `input`, fires ~60 Hz while dragging) produce well over 100 state changes in a few seconds. WebKit throttles pushState/replaceState (100 per 30 s, later 100 per 10 s) and throws `SecurityError: Attempt to use history.replaceState() more than 100 times per …`; an exception thrown inside a passive effect with no ErrorBoundary unmounts the tree → blank page on iPhone/iPad. Chrome silently drops calls instead, so this only bites the mobile-Safari audience. Not verified on a…
  - Fix applied: Debounce the hash write (e.g. `const t = setTimeout(() => { try { if (location.hash !== h) history.replaceState(null,'',h) } catch {} }, 250); return () => clearTimeout(t)`), and wrap in try/catch regardless. Optionally add a top-level ErrorBoundary so no single effect/renderer error blanks the page.
- **[medium] (code) No hashchange listener: navigating to a shared link in an open tab does nothing, then the URL is silently overwritten** — `src/App.tsx`
  - Claim: State→URL is one-way after mount (App.tsx:31-34). Verified: with the app open, setting `location.hash = '#tws=25'` leaves the TWS slider at 12; the next interaction rewrites the hash from state, discarding the pasted scenario. This affects same-document navigations (pasting/clicking a shared link into the already-open tab, editing the hash, back/forward across hash entries created before load).
  - Fix applied: Add `useEffect(() => { const on = () => { const h = location.hash; if (h !== encodeHash(sRef.current)) dispatch({ type: 'patch', patch: decodeHash(h, sRef.current, SLOTS) }) }; addEventListener('hashchange', on); return () => removeEventListener('hashchange', on) }, [])` (keep a ref to the latest state, or add a `load` reducer action). Also compare against the last hash the app wrote to avoid loop…
- **[medium] (code) Lesson patch overrides shared-URL parameters when a lesson link is opened** — `src/App.tsx`
  - Claim: App.init (App.tsx:20) decodes the hash and THEN spreads `LESSONS[step].patch(s)` over it, so any parameter the step's patch touches wins over the URL. Verified: `#tws=20&step=0` loads with TWS = 10 (step 0 patch sets tws:10). Because 'every control stays live' in lesson mode, users tweak values, hit Share, and the recipient sees the default lesson scenario instead — the round-trip test in state.test.ts doesn't cover this path.
  - Fix applied: Apply the lesson patch to the base first and let the URL win: `const s0 = decodeHash(hash, base, SLOTS); const st = s0.lessonStep; return st !== null && LESSONS[st] ? decodeHash(hash, { ...base, ...LESSONS[st].patch(base) }, SLOTS) : s0` (crew from the URL is always fully encoded, so nothing is lost). Add an App-level test or move init() into state.ts to unit-test it.
- **[medium] (code) touch-action:none on the whole deck SVG blocks page scrolling on phones** — `src/index.css`
  - Claim: index.css:111 sets `touch-action: none` on `.deck-svg`. In portrait mode the SVG is `maxHeight: 70vh` and full width, so on a phone the deck occupies most of the first viewport and any swipe that starts on it (hull, water, labels, empty space) does not scroll the page — the user has to find the panel margins to move on. Only the crew markers actually need drag.
  - Fix applied: Move `touch-action: none` to `.deck-svg .crew` (Chromium and WebKit honour touch-action on SVG child elements) and set the svg to `touch-action: pan-y` (or leave default). Verify on a device; if an engine ignores it on `<g>`, put an invisible HTML overlay per marker, or reduce maxHeight to ~50vh as a fallback.
- **[low] (code) Focusable crew markers and lesson dots have role=button but no keyboard behaviour; table inputs share identical aria-labels** — `src/ui/DeckPlan.tsx`
  - Claim: DeckPlan.tsx:159 gives every crew `<g role="button" tabIndex={0}>` with no onKeyDown, so Tab lands on 10 'buttons' that do nothing on Enter/Space (screen readers announce an actionable control that is not). Lesson.tsx:19 dots are `<i role="button" onClick>` without tabIndex/key handling. Readouts.tsx inputs/selects use the same aria-label ('crew name', 'crew weight kg', 'position', 'posture') for all 10 rows, so AT users hear ten identical fields. Slot targets have no accessible name at all.
  - Fix applied: Either drop role/tabIndex from the markers (the crew table is the keyboard path) and add `aria-hidden` to the deck interactions, or add `onKeyDown` (Enter/Space = select/cycle posture, arrows = move to next slot). Give lesson dots `tabIndex={0}` + Enter/Space, or render them as `<button>`. Use `aria-label={`${c.name} weight kg`}` etc. per row.
- **[low] (code) Wall-clock perf assertion gates the Pages deploy** — `src/perf.test.ts`
  - Claim: perf.test.ts:15 asserts derive() < 16 ms per call; locally it is ~5 ms (101 ms for 20 iterations incl. warm-up). deploy.yml runs `npm test` before `npm run build`, and ubuntu-latest runners are 2–3× slower single-thread than an Apple-silicon Mac and noisy under load, so this timing check can intermittently block a deploy on an unrelated commit. Separately, DeckPlan's `layout`/`nearestSlot`, `loadLocal` type validation (kg from tampered localStorage becomes NaN) and App.init are untested.
  - Fix applied: Loosen to a generous bound (e.g. 50 ms) or make it a median-of-N with a `process.env.CI` skip; keep it as a regression signal, not a deploy gate. Export `layout`/`nearestSlot` from DeckPlan (or a `deck.ts`) and add pure tests for the multi-occupant fan-out and snap radius; validate `typeof kg === 'number' && isFinite` in loadLocal.
- **[high] (ux) Live equations are clipped — the bold answers are cut off** — `src/ui/Equations.tsx`
  - Claim: At 1380px the first equation is truncated ("= 1.54 · 0.3" then cut) and at 400px every substituted equation loses its right-hand result ("RM_crew = 850·9.81(…) = −", "⇒ (", "= 40·112·1.34·10.2·0"). `.katex-display{overflow-x:auto}` hides the overflow with no scroll affordance, so the very numbers the panel exists to show (`\mathbf{…kN·m}`, `φ = 23.4°`) are invisible. Seen in step0_1380, tws8_1380, step0_400.
  - Fix applied: Break each long tex into two aligned lines (`\begin{aligned} … \\ &= … \end{aligned}`), putting the substituted numbers and bold result on the second line; drop `.eq-grid` minmax to 300px and set `.eq-grid .katex{font-size:0.95em}` under `@media (max-width:700px)`. If scrolling is kept, add a right-edge fade so users know there is more.
- **[high] (ux) Stern view: F_H label clipped, Δg label under the keel, φ label crossed by the mast; hull couple too small** — `src/ui/HeelSection.tsx`
  - Claim: (a) `F_H … kN` text at `ce.x + 0.15` runs past the viewBox right edge (5.6 m) and is clipped in every screenshot ("F_H 5.1 kl", "F_H 6.4", at 32° only "F_H"). (b) `Δg 103 kN` (end-anchored at gTot.x−0.22, below the weight arrow) sits exactly where the rotated keel fin/bulb (also hull-blue) is drawn, so the fin strikes through the number in all five scenarios. (c) The `φ xx.x°` label starts at radius 2.4 at angle φ/2 and extends right, so the mast line (at angle φ) cuts through it (tws14at, code0). (d) The pedagogic couple G/B/GZ occupies ~150 px of a 660 px panel while the top 60% is an empty …
  - Fix applied: (a) End-anchor F_H to the left of the CE dot when `ce.x > 3.5`, or lower vb.x0/raise vb.w. (b) Put the Δg label to the left of the arrow tail (`x = gTot.x − 0.3, y = gTot.y + 0.4, textAnchor=end`) or draw the keel in a lighter neutral so it never competes with force arrows. (c) Anchor φ label `end` at `x = −0.2` beside the vertical reference, or `middle` at radius 2.9. (d) Crop the sail: set `vb.y…
- **[high] (ux) Crew table on mobile hides the RM column and truncates names** — `src/ui/Readouts.tsx`
  - Claim: At 400px the table overflows horizontally (`overflowX:auto`) so the per-person `RM (kN·m)` column — the panel's whole point, and what lesson step 4 tells users to look at — is off-screen with no scroll cue, `arm y` is half cut ("+0.0("), and name inputs collapse to "Crev". Confirmed in step0_400.
  - Fix applied: Under `@media (max-width:700px)`: hide the `arm y` column, cap `table.tbl input{width:5.5em}` for names, and move the RM column to directly after Name (or render RM as a small bar+number under the name). Alternatively hide the Posture select on mobile since posture can be cycled on the deck plan.
- **[medium] (ux) Deck-plan label collisions (below-decks fan, mobile rail/wind labels) and wrong hint on mobile** — `src/ui/DeckPlan.tsx`
  - Claim: (a) 'Below decks' fan is 10×0.58 = 5.2 m centred at x=7.6, spanning 5.0–10.2 m: crew 1 covers the mast dot (x=5.3) and 'mast' label, crew 9/10 sit on top of the Pit (9.7) / Lee pit slots and their labels; the 'Below decks' label is drawn under crew 5/6 (visible as 'Belo|decks' at 400px). (b) In vertical mode the rotated 'WIND' text is drawn on top of its own arrow shaft, 'windward rail' is behind rail crew 5/6 ('dward rail'), 'leeward rail' crosses the hull outline, and 'Lee pit'/'Pit' collide. (c) The panel hint says 'wind from the top' but on mobile the wind arrow comes from the right. (d) T…
  - Fix applied: (a) Fan below-deck crew in two rows (`dx = (k%5 − 2)*0.6, dy = k<5 ? 0.35 : −0.35`) so the group stays inside the coachroof (5.6–9.2 m), and hide the MULTI slot label when occupied. (b) In vertical mode move 'WIND' beside the arrow (offset x by 0.45 after rotation), place rail labels outside the hull (y = ±(halfBeam+0.55)) and offset 'Lee pit' vertically. (c) Make the hint conditional: `vertical ?…
- **[medium] (ux) Wind-sweep chart: annotation collides with curves, 50° plateau reads as saturation, free-wind gap not drawn** — `src/ui/Charts.tsx`
  - Claim: (a) The 'extra TWS at 20° heel vs. all crew inboard' caption at (M.l+8, M.t+31) is crossed by both the grey dashed and amber curves in every screenshot. (b) Overpowered points are drawn at yMax=50 so both lines flatten into a horizontal shelf that looks like heel saturates and both formations converge — the opposite of the message; only a footnote explains it. (c) The headline 'free wind' number has no visual counterpart: nothing marks the horizontal gap between the two curves at the target-heel line.
  - Fix applied: (a) Move the two annotation lines to bottom-right (`x=w−M.r−4, textAnchor=end, y=H−M.b−26/−12`), which is empty. (b) Stop the path at the last non-overpowered point (`linePath` with NaN break) and shade the region beyond with a light red hatch labelled 'overpowered'. (c) Draw a short horizontal bracket on the target-heel line from `sx(tb)` to `sx(ta)` (values already computed via `twsAtHeel`) in a…
- **[medium] (ux) Desktop layout leaves ~300 px of blank column under the deck plan** — `src/index.css`
  - Claim: With `align-items:start` and areas "deck section", the deck panel is ~370 px tall while the stern view is ~660 px, so in sandbox mode (tws14at_1380, code0_1380, tws8_1380) there is a large empty region under the deck plan above the full-width stats row.
  - Fix applied: Change the grid areas so the left column stacks: `"lesson lesson" "deck section" "stats section" "controls readouts" …` (stats and deck share the 3fr column; section spans two rows) — the stat tiles already reflow via auto-fit. Or put controls there: `"deck section" "controls section" "stats readouts"`.
- **[low] (ux) Moment chart φ label overlaps the heeling-moment curve; RM-total tile colour contradicts chart encoding** — `src/ui/Charts.tsx`
  - Claim: The equilibrium label `φ = 23.4°` is placed at (+8, −8) from the crossing, which is exactly where the red HM curve continues, so the red line strikes through the digits (step0, step4, tws8 screenshots). Separately, the 'RM total' stat tile is `.hull` (blue) while the chart's total-RM curve and legend are amber, and 'GM · G shift' is teal (buoyancy) though it reports a hull property.
  - Fix applied: Give the label a white halo (`paintOrder='stroke' stroke='#fff' strokeWidth={3}`) and place it below-right of the dot (`y = sy(eqY) + 18`) where both curves have moved away. Make the RM-total tile amber (`.stat.crew` or a neutral ink border) and the GM tile hull-blue so tile colours match the curves.

## Refuted (not changed)
- (realism) Lesson steps show 26–37° heel in 12–16 kn as 'the heel angle you actually sail at' — miscalibrated vs the doc's own 20–22° target
  - Refuter: Recomputed with vite-node against the real code (src/physics/*, src/data/xp44.json): the reviewer's numbers are exact — TWA 40, flat=1: 12 kn → 25.2° hike / 26.3° sit / 29.1° below; 14 kn → 31.3/32.2/…
- (realism) Posture offsets applied on the leeward rail produce a 'Full hike to leeward' pose
  - Refuter: Facts check out (crewPositions uses Math.sign(s.y)·off so a lee-rail 'hike' lands at y ≈ −(1.68+0.4); Readouts shows the posture select for any kind==='rail' slot; moveCrew keeps posture), but this is…
- (realism) RRS 49.2 paraphrase drops the qualifying conditions
  - Refuter: The app text is a deliberate one-clause paraphrase in a caveats bullet (src/ui/Advanced.tsx:35) and a lesson step (src/data/lessons.ts:41); it is not wrong, just condensed. RRS 49.2's second sentence:…
- (realism) Seed polar is ~5–8% slow upwind for an Xp 44
  - Refuter: Not a defect. (1) The polar is explicitly a seed: xp44.json polar._notes already says "Seed polar (approx. for a 44 ft racer-cruiser). Replace with ORC VPP polar", the Advanced panel assumptions list …
- (ux) Deep-linking to a lesson step applies only that step's patch, so shared step URLs show the wrong scenario
  - Refuter: The premise is wrong. `encodeHash` (src/state.ts) always serialises the full scenario — tws, twa, flat, at, th, sm, z and the complete `crew` slot/posture list — and only additionally sets `step` when…

## Not applied / partially applied
- Calibration (rejected as a defect): full-power heel at 12–14 kn is 24–30°; lesson steps now use 10–12 kn and switch to auto-trim earlier, and prose says explicitly that trimmers would depower.
- Seed polar bumped ~+0.3 kn upwind (refuted as a defect but cheap and closer to public Xp 44 polars).
- RRS 49.2 wording expanded in the Advanced panel and lesson step 4.
- Lee-rail posture: offsets now apply on the windward rail only; posture select hidden for other rows.
