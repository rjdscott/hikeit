# Adversarial review #2 — 2026-08-18

Round-2 pass after the round-1 fixes: 3 reviewer lenses (physics recheck, UX on the live site, code diff) → 2 independent refuters per finding. 15 findings, 13 confirmed, 2 refuted. All confirmed items fixed in the follow-up commit.

## Confirmed → fixed
- **[medium] (physics) Twist function lowers CE measured from the waterline instead of from HBI (deck), overstating depower by up to 4 %** — `src/physics/aero.ts (zceEff / heelingArm), src/data/xp44.json (sails[].ce)`
  - Claim: ORC eq 5.49 applies the twist factor to Z_CE above HBI, and eq 5.57 forms the arm as HBI + Z_CE·REEF; the freeboard is not part of what twists. `heelingArm` calls `zceEff(b.zce, …)` where `zce` is the area-weighted CE ABOVE THE WATERLINE (9.26 m, incl. 1.2 m freeboard), so the freeboard is scaled by the twist factor too. Recomputed: flat 0.76 (auto-trim, 14 kn/20°) → arm 9.24 m vs ORC-consistent 9.37 m (HM 1.4 % low); flat 0.60 → 8.57 vs 8.79 (2.5 % low); flat 0.42 → 7.82 vs 8.14 (3.9 % low). Ev…
  - Fix: In `heelingArm`/`zceEff`, twist only the part above the sheer: `zceEff(b.zce - fb, flat, b.frac) + fb + 0.43·draft` with `fb = json.hull.freeboard` (or store `ce` in the JSON above HBI and add freeboard when resolving); update the Equations caption if it describes the arm.
- **[low] (physics) Quadratic parasite term still 0.005 although review #1 records KPP≈0.014 as applied — drive force ~3 % high** — `src/physics/aero.ts:32`
  - Claim: docs/reviews/2026-08-18-adversarial-review-1.md lists 'set the quadratic parasite constant to ~0.014 (main 0.0138, jib ~0.016 area-weighted)' under 'Fix applied', but `cdi = cl*cl*(1/(π·ar) + 0.005)` is unchanged. ORC 2023 Table 5.1/5.3 give kpmm 0.01379, kpj 0.016 (eq 5.41/5.46: CE = KPP + Aref/(π·heff²)). Recomputed at AWA 26° (14 kn/40°): C_D 0.125 → 0.143, C_R 0.516 → 0.499 (−3.1 %), C_H +0.6 %. So the 'Drive force' tile and the posture-panel drive % are a few percent high and the review log…
  - Fix: Use an area-weighted KPP (≈0.0138·A_main + 0.016·A_jib)/A ≈ 0.0147 in place of 0.005 (or a per-sail `kpp` field in xp44.json), re-baseline the C_H/C_R test band; or amend the review log to say the KPP change was not applied.
- **[low] (physics) Stern-view heeling-arm dimension is drawn as h·cosφ but labelled with h** — `src/ui/HeelSection.tsx:118-121`
  - Claim: F_H and the keel reaction are drawn as horizontal (earth-frame) arrows at CE and CLR, and the 'h … m → HM …' dimension line spans the vertical screen distance between them, which is h·cosφ, while the label prints d.arm (boat-frame arm). At the 14 kn step (φ = 29.4°) the drawn span is 8.9 m but the label reads 10.2 m; at 23.6° it is 9.4 m vs 10.2 m. A reader checking F_H × h against the picture gets a different arm from the one that produces the printed HM (the model's HM = F_H·h·cos²φ, force nor…
  - Fix: Either draw the force arrows and dimension line in the boat frame (inside the rotate(φ) scale(−1,−1) group, arrows normal to the mast, arm along the mast) so the drawn arm is h, or keep the earth-frame drawing and label it 'h·cosφ = … m' (and note cos²φ in the legend).
- **[low] (physics) Posture panel counts leeward-rail crew as 'on the rail' and offers postures that have no effect** — `src/model.ts:69 (railCount), src/ui/Posture.tsx:16-19`
  - Claim: crewPositions applies posture offsets only when `s.side === 'w'` (round-1 fix), but `railCount` and PosturePanel's `railSlots` filter on `kind === 'rail'` only. With the 'Light air: leeward' preset the panel reads '8 crew on the rail (680 kg)', the Sitting/Legs over/Full hike buttons are enabled, and the table shows three identical rows (rmCrew −12.28 kN·m, heel 12.7°, free wind −1.5 kn each) — misleadingly implying hiking to leeward is being evaluated. Recomputed: derive(...).railCount = 8 for …
  - Fix: Filter on `kind === 'rail' && side === 'w'` in both places (a `isWindwardRail(slot)` helper), so the panel shows the 'Nobody is on the rail' message and railCount = 0 when crew are only on the lee rail.
- **[high] (ux) Deep-linked #step=6 does not turn on auto-trim: decodeHash discards the lesson patch's autoTrim/zPenalty** — `src/state.ts (decodeHash), src/App.tsx (fromHash)`
  - Claim: Live https://rjdscott.github.io/hikeit/#step=6 renders lesson 7 'Or: hike, and the trimmers give you power' with the Heel tile at 30.3°, Drive 'flat 1.00', and the Sitting-vs-hiking table in heel mode (not power/drive) — i.e. autoTrim is off although LESSONS[6].patch sets autoTrim:true, targetHeel:20. Cause: decodeHash sets `autoTrim: p.get('at') === '1'` and `zPenalty: p.get('z') !== '0'` unconditionally, so when the param is absent the patched base is overwritten (the round-1 'URL wins' fromHa…
  - Fix: In decodeHash: `autoTrim: p.has('at') ? p.get('at') === '1' : base.autoTrim`, `zPenalty: p.has('z') ? p.get('z') !== '0' : base.zPenalty`. Add a state.test case: decodeHash('#step=6', {...base, autoTrim:true}) keeps autoTrim. For bare step links, make fromHash fold prior steps' patches (reduce LESSONS.slice(0,step+1)) so #step=6 lands on the hiking…
- **[high] (ux) Stern view loses the sail couple as heel grows: F_H label clipped from ~22°, CE/F_H arrow entirely off-canvas above ~33°; Δg still struck through by the keel** — `src/ui/HeelSection.tsx`
  - Claim: Live crops: code0 (#twa=90&sm=code0&tws=14, φ=23.9°) shows 'F_H 6.6 k'; light-air-leeward (φ=27.6°) shows 'F_H 4.'; #tws=22 (φ=47.3°) has no CE dot, no F_H arrow and no F_H label at all — only the keel arrow and the vertical 'h 10.2 m → HM 90.2' dimension survive, and the top 40% of the panel is blank. Reason: ce = S(0, zce) → x = 10.2·sinφ, but the viewBox is fixed at x0=-5.6, w=11.2, and the F_H text is always drawn at ce.x+0.25 (the round-1 'end-anchor when ce.x > 3.5' was not applied). The p…
  - Fix: Derive the viewBox from the geometry: `const xMax = Math.max(5.6, ce.x + fixedS + 2.0); vb = { x0: -5.6, w: 5.6 + xMax, ... }` (or keep w and scale down via preserveAspectRatio), and end-anchor F_H left of the CE dot when ce.x > 3.2. Put Δg at the bottom of the weight arrow but offset to the leeward side of the keel (`x = gTot.x + 0.35, textAnchor=…
- **[medium] (ux) Wind-sweep chart: 'overpowered →' clipped to 'overp', 50° clamp still draws a false plateau, unlabeled ghost vertical** — `src/ui/Charts.tsx (WindSweepChart)`
  - Claim: At default and #tws=22 (flat 1.00) the live chart shows the hatch starting at 29 kn with the label truncated to 'overp' at the right edge (text at sx(overFrom)+6, start-anchored, past the plot). Between 24 and 28 kn the orange curve is a flat horizontal shelf at exactly 50° because deg() does Math.min(yMax, φ) for non-overpowered points — the round-1 fix only NaN-breaks the overpowered points, so heel that is really 52–58° reads as 'saturates at 50°, both formations converge'. The dashed grey ve…
  - Fix: `deg = p => p.overpowered || p.phi/DEG > yMax ? NaN : p.phi/DEG` (or raise yMax to 60 and let it show); anchor the label `textAnchor='end' x={w-M.r-4}` when sx(overFrom) > w − M.r − 90; either drop the overFromBase line or label it 'inboard overpowered' in ghost grey.
- **[medium] (ux) Live equations still clipped at 400 px — the bold results are cut off in 4 of 5 lines** — `src/ui/Equations.tsx, src/index.css (.eq-grid @media)`
  - Claim: Live default at 400 px, 'The maths, live' panel: GZ line ends at '1.54 · 0.318 · 0.950' (no '= 0.464 m'); RM_crew second line ends '= +1' (should be +12.1 kN·m); C_H line ends '0.12 · 0.4' (no '= 1.31'); HM second line ends '= 56' (no '.2 kN·m'); the last line ends '⇒' with 'φ = 18.5°' missing. .katex-display{overflow-x:auto} still gives no scroll cue. The round-1 two-line split fixed 1380 but the substituted lines are still ~420 px wide at 0.92em on a 336 px column.
  - Fix: Under @media (max-width:700px) split each substituted line again at the final '=' (`\\ &= \mathbf{…}` as a third aligned row) so the answer stands alone, and set `.eq-grid .katex{font-size:0.82em}`; add a right-edge fade on .katex-display when scrollWidth > clientWidth.
- **[medium] (ux) Sitting vs hiking is not visually obvious: tiny stern-view figure obscured by CG dots, text-only panel, and three different lever-arm percentages on screen at once** — `src/ui/HeelSection.tsx (Figure), src/ui/Posture.tsx, src/data/lessons.ts step 4, src/ui/DeckPlan.tsx`
  - Claim: The rail figure is ~55 px tall at 1380 and ~30 px on mobile because the 14.3 m-tall viewBox is dominated by the rig; in the sitting pose the helm/trimmer CG dots (y=1.27/0.96) land on the figure's legs, so the default view is an orange blob that does not read as a person, and only the head position differs between sit and hike. The 'Sitting vs hiking' panel is a table with no drawing of the arm. Meanwhile the same quantity is stated three ways in one viewport at #step=3: lesson prose '+20% on th…
  - Fix: Draw the two figures (sit vs hike, side by side, with a dimensioned arm from centreline) inside the Posture panel at ~120 px, or crop the stern view (vb.y0 ≈ −4.5 with a broken mast) and hide the helm/trimmer dots under the figure. Compute the % from POSTURE_OFFSET.hike/(halfbeamAt(7.5) − rail.inset) and template the lesson number from the same exp…
- **[high] (code) touch-action:none on the SVG <g class=crew> is ignored — vertical touch drags are cancelled by the browser** — `src/index.css (.deck-svg / .deck-svg .crew) + src/ui/DeckPlan.tsx`
  - Claim: Round-1 moved touch-action:none from .deck-svg to .deck-svg .crew and set the svg to pan-y. Chromium does not honour touch-action on SVG child elements, so any finger drag on a crew marker with a vertical component is taken over by page scrolling: the marker gets 3 pointermoves then `pointercancel` and snaps back. Verified with CDP Input.dispatchTouchEvent on a 400px mobile viewport (portrait deck): drag Crew 1 down the rail to Rail 7 → pointercancel, hash unchanged; same drag horizontally (wind…
  - Fix: Keep the svg at pan-y for scrolling but stop the browser claiming crew drags with a native non-passive listener: in DeckPlan `useEffect(() => { const el = svgRef.current; const h = (e: TouchEvent) => { if ((e.target as Element).closest('.crew')) e.preventDefault() }; el.addEventListener('touchstart', h, { passive: false }); return () => el.removeEv…
- **[medium] (code) Corrupt/legacy localStorage crashes the app permanently — 'Reset and reload' does not clear it** — `src/state.ts (loadLocal) + src/App.tsx (Boundary)`
  - Claim: loadLocal copies `name`/`kg` from localStorage without type checks (round-1 listed `typeof kg === 'number' && isFinite` as part of the fix; not applied). Verified: `hikeit.v1 = {crew:[{name:5,kg:85}]}` → `c.name.trim is not a function` in DeckPlan initials → Boundary; clicking 'Reset and reload' clears only the hash and reloads into the same crash (loop until the user manually clears storage). `kg:'abc'` renders Heel −55.7°, RM crew '–'. Any future change to the stored shape hits every returning…
  - Fix: In loadLocal validate per field: `name: typeof n === 'string' ? n.slice(0,40) : c.name`, `kg: typeof k === 'number' && Number.isFinite(k) ? clamp(40,150,k) : c.kg`; make Boundary's reset also `localStorage.removeItem('hikeit.v1')` (or saveLocal wrapper key) before reload. Add a state.test case for a tampered payload.
- **[medium] (code) Posture panel treats leeward-rail crew as rail crew and hardcodes the old 0.35 m inset** — `src/ui/Posture.tsx (railSlots, footnote) + src/model.ts (railCount, postures)`
  - Claim: ADR-0010: posture offsets apply on the windward rail only (crewPositions ignores posture when side==='l'). But Posture.tsx builds railSlots from `kind==='rail'` (both sides), model.railCount and the postures what-if do the same. Verified with derive(): 'Light air: leeward' preset → panel says '8 crew on the rail (680 kg)', buttons enabled, and the sit/legs/hike rows are three identical lines (27.57° each) — the 'set all' button also silently writes posture onto lee crew, so `current` (requires A…
  - Fix: Use `x.kind==='rail' && x.side==='w'` for railSlots in Posture.tsx and for railCount/postures in model.ts (App.tsx already does this for railPosture); compute the percentage from a real slot arm, e.g. `POSTURE_OFFSET.hike / d.boat.slotById['rail-w-4'].y`, and read the beam from json.hull.beam.
- **[low] (code) Deck-plan keyboard shortcuts fire on browser chords (Cmd+L, Ctrl+B) and aria-label promises keys that do nothing off the rail** — `src/ui/DeckPlan.tsx (onKeyDown, aria-label)`
  - Claim: onKeyDown maps 'w'/'l'/'b' to moves without checking modifiers. Verified: focus a crew marker (Tab), press Meta+L (address bar) → crew moved to Lee rail 5; Ctrl+B → crew moved below. Also the aria-label on every crew announces 'Enter: cycle posture, arrows: move along the rail' even for below-decks/cockpit crew, where Enter and arrows are no-ops (rails=[] → idx −1) — screen-reader users are told about controls that don't work.
  - Fix: Early-return when `e.metaKey || e.ctrlKey || e.altKey`; build the aria-label hint conditionally (rail-w: 'Enter: cycle posture, arrows: move along the rail'; any: 'W/L/B: windward rail, leeward rail, below').

## Refuted (kept as-is)
- (physics) 'All crew inboard' baseline sits at z = 1.3 m, not the certificate reference z₀ = 0.98 m, so 'free wind' is inflated ~7 %
  - Refuter: Numbers check out (recomputed with vite-node against src/model.ts at 14 kn / TWA 40 / target 20°, hiking preset: free wind 1.95 kn with baseline z=1.3 vs 1.83 kn with z=zCrew0=0.98; sitting preset 1.6…
- (code) Tap-to-place onto an occupied slot selects the occupant instead of moving/swapping
  - Refuter: Facts reproduce (Playwright, mouse and touch: select Crew 1, tap Crew 2 -> legend 'Crew 2: Rail 2', crew hash unchanged; DeckPlan.tsx onUp line 92 `else setSelected(c.id)`), but this is a design choic…

## Notes
- Heeling arm: twist now applied only to the sail plan above the sheer (ORC 5.49/5.57); ORC KPP parasite term is per-sail in `xp44.json` (main 0.0138, jib 0.016, Code 0 0.019).
- Stern view: canvas widens with heel so the CE/F_H couple never leaves the frame; the vertical dimension is labelled h·cosφ with h in brackets.
- Sitting-vs-hiking: three dimensioned deck-edge figures added to the posture panel; windward-rail-only counting; lesson prose no longer quotes a percentage that could drift from the data.
- Deep links: `#step=n` folds all previous steps (`lessonStateAt`), and `at`/`z` params only override when present.
- Mobile drag: native non-passive `touchstart` preventDefault on crew markers (Chromium ignores `touch-action` on SVG children); the SVG stays `pan-y` so the page still scrolls.
- localStorage payloads are validated; error boundary reset also clears storage.
